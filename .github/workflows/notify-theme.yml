name: Notify Theme Repo

on:
  push:
    branches: [main]

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.THEME_REPO_TOKEN }}
          repository: ak0r/zero-theme
          event-type: content-updated
